/*
 * Operadore de String (String Operator)
 */

// comparison (comparação)

console.log('a' == 'a') //retorna true
console.log('a' == 'b') //retorna false

//concatenation (concatenação)
// retorna a união de duas Strings
//Exemplo 1:
let alpha = 'alpha'
console.log(alpha + 'bet') //retorna alphabet

//Exemplo 2: Atualizar a variável
let alpha = 'alpha'
alpha += 'bet'
console.log(alpha) //retorna alphabet
